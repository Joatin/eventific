import { ViewHandler } from './ViewHandler';
import { IViewHandler } from './IViewHandler';


test('it should be defined', async () => {
  expect(ViewHandler).toBeDefined();
  expect(IViewHandler).toBeDefined();
});
